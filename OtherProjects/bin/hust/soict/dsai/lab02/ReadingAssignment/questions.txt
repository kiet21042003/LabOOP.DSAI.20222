No, Java does not allow you to create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Here, we try to overloading methods that have the same numbers and the same types of arguments (title and category are all String), which is not allowed.